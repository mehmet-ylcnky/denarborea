coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%
    patch:
      default:
        target: 70%
        threshold: 1%

ignore:
  - "tests/"
  - "target/"
  - "*.md"

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: false
